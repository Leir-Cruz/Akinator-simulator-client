import React from 'react'
import { Routes, Route } from 'react-router-dom'

const RoutesList = () => {
    return (
        <Routes>
            <Route path='/' element={} />
        </Routes>
    )
}

export default Routes